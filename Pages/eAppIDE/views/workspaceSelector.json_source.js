/*function WorkspaceSelector(parent) {
    //QDialog.call(this, parent);
}

WorkspaceSelector.prototype = vdom.ui.load("ui.WorkspaceSelectorDialog");

WorkspaceSelector.prototype.setWorkspaceList = function(entries) {
    this.workspaceList.addItems(entries);
}

WorkspaceSelector.prototype.getSelected = function() {
    return this.workspaceList.currentText();
}

WorkspaceSelector.prototype.setLoading = function() {

}

Object.defineProperties(WorkspaceSelector.prototype, {
    title: {
        get: function() {
            return this.windowTitle;
        },
        set: function(value) {
            this.windowTitle = value;
        }
    },
    text: {
        get: function() {
            return this.label.text;
        },
        set: function(value) {
            this.label.text = value;
        }
    }
});*/
